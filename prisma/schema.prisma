// This is your Prisma schema file,
// To properly update db on PlanetScale if changes are made to the table: npx prisma db push (can only be done if database is not in production)
// To view database in browser: 'npx prisma studio'

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Review {
  id         Int    @id @default(autoincrement())
  firstName  String
  lastName   String
  reviewText String
}

/// --- Supabase Heartbeat (run in Supabase SQL editor, not Prisma) ---
/// The following SQL creates a `heartbeat` table used for periodic pings to keep the project active.
/// Keep this here for documentation and easy copy/paste.
///
/// create table if not exists public.heartbeat (
///   id bigint generated by default as identity primary key,
///   created_at timestamptz not null default now(),
///   source text not null default 'vercel-cron',
///   note text
/// );
///
/// create index if not exists heartbeat_created_at_idx on public.heartbeat (created_at desc);
